#nullable enable

// Copy to BuildInjectedSecrets.local.cs, which is ignored by Git.
// If that file already exists, merge the needed methods instead of replacing it.
// This template contains no credentials and is not compiled as a .cs.example file.
namespace BuildSecrets
{
    public static partial class BuildInjectedSecrets
    {
        static partial void SetCompanionAppClientId ( ref string value )
        {
            // Assign your authorized Companion API client ID here.
            // Leave unchanged if this build does not provide one.
        }

        static partial void SetTelemetryApiKey ( ref string value )
        {
            // Assign your authorized telemetry API key here.
            // Leave unchanged if this build does not provide one.
        }

        // To enable EDDN signing, add the following block after replacing the key:
        // #if false
        // private const string EddnSigningKeyInput = @"original-key-from-EDDN";
        // #endif
        // Remove the leading // from those three lines when adding the block.
        // Omit the block entirely for unsigned builds; do not use an empty key.
        // The build generates SetEddnSigningKey automatically. Do not define it here.
    }
}
