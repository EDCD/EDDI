<UserControl x:Class="EddiGalnetMonitor.ConfigurationWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:resx="clr-namespace:EddiGalnetMonitor.Properties"
            mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="600">
    <DockPanel LastChildFill="False" Background="{DynamicResource WindowBackgroundBrush}" Margin="0,5">
        <Grid DockPanel.Dock="Top" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,5">
                <TextBlock TextWrapping="Wrap" Text="{x:Static resx:GalnetMonitor.language_label}" VerticalAlignment="Center"/>
                <ComboBox x:Name="languageComboBox" SelectionChanged="onLanguageChanged" SelectedValue="Key" VerticalAlignment="Center" Padding="5" Margin="5, 0" MinWidth="100"/>
            </StackPanel>
            <TextBlock Grid.Row="1" Margin="0,5" TextWrapping="Wrap" Text="{x:Static resx:GalnetMonitor.p1}" VerticalAlignment="Center"/>
            <StackPanel Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,10">
                <CheckBox x:Name="galnetAlwaysOnCheckBox" HorizontalAlignment="Left" VerticalAlignment="Center" Checked="galnetAlwaysOnChecked" Unchecked="galnetAlwaysOnUnchecked"/>
                <TextBlock TextWrapping="Wrap" Text="{x:Static resx:GalnetMonitor.p2}" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </DockPanel>
</UserControl>
