<UserControl x:Class="EddiGalnetMonitor.ConfigurationWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:resx="clr-namespace:EddiGalnetMonitor.Properties"
            mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="600">
    <DockPanel LastChildFill="False" Background="{DynamicResource WindowBackgroundBrush}" Margin="0,5">
        <Grid DockPanel.Dock="Top" Margin="0, 10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="5,0,5,10">
                <TextBlock Margin="0,0,10,0" TextWrapping="Wrap" Text="{x:Static resx:GalnetMonitor.language_label}" VerticalAlignment="Center"/>
                <ComboBox x:Name="languageComboBox" SelectionChanged="onLanguageChanged" SelectedValue="Key" VerticalAlignment="Center"/>
            </StackPanel>
            <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="5,0,5,15" TextWrapping="Wrap" Text="{x:Static resx:GalnetMonitor.p1}" VerticalAlignment="Top"/>
            <CheckBox x:Name="galnetAlwaysOnCheckBox" Grid.Row="2" Grid.Column="0" Margin="5,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Center" Checked="galnetAlwaysOnChecked" Unchecked="galnetAlwaysOnUnchecked"/>
            <TextBlock Grid.Row="2" Grid.Column="1" Margin="5,0,5,0" TextWrapping="Wrap" Text="{x:Static resx:GalnetMonitor.p2}" VerticalAlignment="Center"/>
        </Grid>
    </DockPanel>
</UserControl>
