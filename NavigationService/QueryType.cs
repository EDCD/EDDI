namespace EddiNavigationService
{
    public enum QueryType
    {
        // ReSharper disable once UnusedMember.Global
        None, // null

        cancel,
        encoded,
        expiring,
        facilitator,
        farthest,
        guardian,
        human,
        manufactured,
        most,
        nearest,
        raw,
        route,
        scoop,
        set,
        source,
        update
    }
}
