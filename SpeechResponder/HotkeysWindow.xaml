<Window x:Class="EddiSpeechResponder.HotkeysWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EddiSpeechResponder"
        xmlns:resx="clr-namespace:EddiSpeechResponder.Properties"
        mc:Ignorable="d"
        Title="Configure Hotkeys" 
        FocusManager.FocusedElement="{Binding ElementName=actionComboBox}"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        KeyDown="RegisterHotkeyButton_KeyDown"
        KeyUp="RegisterHotkeyButton_KeyUp"
        Height="150" Width="300"
        >
    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <!--<RowDefinition Height="*"/>-->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Column="0" Grid.Row="0" VerticalAlignment="Center" Text="Action:" Margin="5" />
        <ComboBox x:Name="actionComboBox" Grid.Column="1" Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="5" MinWidth="200" SelectionChanged="ActionComboBoxOnSelectionChanged"/>
        <TextBlock x:Name="hotkeyTextBlock" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" HorizontalAlignment="Center" TextAlignment="Center" VerticalAlignment="Center" Margin="5" MinWidth="200" />
        <!--<Button x:Name="registerHotkeyButton" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="2" HorizontalAlignment="Center" HorizontalContentAlignment="Center" VerticalAlignment="Center" Margin="5" MinWidth="200" IsEnabled="False" Content="Register hotkey"  Click="RegisterHotkeyButton_Click" KeyDown="RegisterHotkeyButton_KeyDown" KeyUp="RegisterHotkeyButton_KeyUp"/>-->
        <UniformGrid Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="2" Rows="1" Columns="2" HorizontalAlignment="Center" Margin="5">
            <Button x:Name="acceptButton" IsDefault="True"
                    Content="{x:Static resx:SpeechResponder.button_ok}" 
                    VerticalAlignment="Top" 
                    Click="acceptButtonClick" 
                    Margin="10" MinWidth ="90"/>
            <Button x:Name="cancelButton" IsCancel="True"
                    Content="{x:Static resx:SpeechResponder.button_cancel}" 
                    VerticalAlignment="Top" 
                    Click="cancelButtonClick" 
                    Margin="10" MinWidth ="90"/>
        </UniformGrid>
    </Grid>
</Window>
